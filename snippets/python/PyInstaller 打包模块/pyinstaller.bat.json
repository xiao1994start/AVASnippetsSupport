{
  "pip|uv PyInstaller 打包库": {
    "prefix": ["pip PyInstaller", "uv PyInstaller"],
    "body": [
      "ECHO.",
      "ECHO * PyInstaller 打包库 *",
      "pip install \"pyinstaller\" --upgrade -i https://mirrors.aliyun.com/pypi/simple/",
      "uv add \"pyinstaller\" --upgrade --index https://mirrors.aliyun.com/pypi/simple/",
      ""
    ],
    "description": ["PyInstaller 打包库"]
  }
}
