{
  "引入 urllib URL处理模块": {
    "prefix": [
      "import urllib.request -saveLocal",
      "imp urllib.request -saveLocal",
      "import urllib.request -download",
      "imp urllib.request -download"
    ],
    "body": ["import urllib.request  # 引入 urllib URL处理模块", ""],
    "description": ["引入 urllib URL处理模块"]
  }
}
