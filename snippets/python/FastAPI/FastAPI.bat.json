{
  "pip|uv FastAPI 网络API接口后端库": {
    "prefix": ["pip fastapi", "uv fastapi"],
    "body": [
      "ECHO.",
      "ECHO * FastAPI 构建 API 接口后端 Web 框架 *",
      "@REM pip install \"fastapi[standard]\" --upgrade -i https://mirrors.aliyun.com/pypi/simple/",
      "uv add \"fastapi[standard]\" --upgrade --index https://mirrors.aliyun.com/pypi/simple/",
      ""
    ],
    "description": ["FastAPI 网络API接口后端库"]
  }
}
