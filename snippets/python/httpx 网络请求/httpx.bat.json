{
  "pip|uv httpx 网络请求库": {
    "prefix": ["pip_httpx", "uv_httpx"],
    "body": [
      "ECHO.",
      "ECHO * httpx 网络请求库 *",
      "pip install \"httpx\" \"httpx[http2]\" \"httpx[brotli]\" \"httpx[socks]\" --upgrade -i https://mirrors.aliyun.com/pypi/simple/",
      "uv add \"httpx\" \"httpx[http2]\" \"httpx[brotli]\" \"httpx[socks]\" --upgrade --index https://mirrors.aliyun.com/pypi/simple/",
      ""
    ],
    "description": ["httpx 网络请求库"]
  }
}
