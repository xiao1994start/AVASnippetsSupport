{
  "try:正常执行 except:异常处理": {
    "prefix": ["try except =-try"],
    "body": [
      "try:",
      "    $0",
      "    ...",
      "except Exception as e:",
      "    print(f'错误类型:{type(e)}')",
      "    print(f'错误信息:{e}')",
      "    ...",
      "    raise error  # raise: 抛出异常, 程序终止 (如果注释掉, 则程序继续执行, 正常退出)"
    ],
    "description": ["try:正常执行 except:异常处理"]
  },
  "except 抛出异常": {
    "prefix": ["except as e =-try"],
    "body": [
      "except Exception as e:",
      "    print(f'错误类型:{type(e)}')",
      "    print(f'错误信息:{e}')",
      "    ...",
      "    raise e  # raise: 抛出异常, 程序终止 (如果注释掉, 则程序继续执行, 正常退出)",
      "    return",
      ""
    ],
    "description": ["except 抛出异常"]
  },
  "except KeyboardInterrupt 按键终止": {
    "prefix": ["except KeyboardInterrupt ="],
    "body": [
      "except KeyboardInterrupt:",
      "    subprocess.run('@ECHO OFF && ECHO 按键终止程序... && PAUSE', shell=True)",
      "    ...",
      ""
    ],
    "description": ["except KeyboardInterrupt 按键终止"]
  },
  "except PermissionError 文件操作": {
    "prefix": ["except PermissionError"],
    "body": [
      "except PermissionError:",
      "    subprocess.run('@ECHO OFF && ECHO 文件操作权限不足... && PAUSE', shell=True)",
      "    ...",
      ""
    ],
    "description": ["except PermissionError 文件操作"]
  }
}
