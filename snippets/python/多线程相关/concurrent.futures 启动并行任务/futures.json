{
  "引入 concurrent.futures 启动并行任务": {
    "prefix": ["import futures", "im futures"],
    "body": [
      "from concurrent.futures import ProcessPoolExecutor, ThreadPoolExecutor, as_completed  # * 启动并行任务[ThreadPoolExecutor:线程池; ProcessPoolExecutor:进程池]",
      ""
    ],
    "description": ["引入 concurrent.futures 启动并行任务"]
  },
  "创建 ThreadPoolExecutor 线程池": {
    "prefix": ["with ThreadPoolExecutor as -concurrent.futures"],
    "body": [
      "with ThreadPoolExecutor(max_workers=6) as executor:",
      "    \"\"\"# ThreadPoolExecutor - 多线程：IO密集型任务（网络、文件、等待外部程序）",
      "    **创建 ThreadPoolExecutor 线程池实 例对象 executor**",
      "    futures = [executor.submit(fn, *args, **kwargs)] : fn 参数是要执行的函数, *args 和 **kwargs 是 fn 的参数",
      "    results = [] : 使用空列表接收线程运行结果 -> [results.append(future.result()) for future in as_completed(futures)]",
      "    \"\"\"",
      "    results = []  # 记录返回结果",
      "    futures = [executor.submit(do_work, i) for i in range(5)]",
      "",
      "    for future in as_completed(futures):",
      "        result = future.result()",
      "        results.append(result)",
      "",
      "print(results)",
      ""
    ],
    "description": ["创建 ThreadPoolExecutor 线程池"]
  },
  "创建 ProcessPoolExecutor 进程池": {
    "prefix": ["with ProcessPoolExecutor as -concurrent.futures"],
    "body": [
      "with ProcessPoolExecutor(max_workers=6) as executor:",
      "    \"\"\"# ProcessPoolExecutor - 多进程：CPU密集型任务（计算、循环、解析、音频、视频、图像、压缩、解压、处理）",
      "    **创建 ProcessPoolExecutor 进程池 实例对象 executor**",
      "    futures = [executor.submit(fn, *args, **kwargs)] : fn 参数是要执行的函数, *args 和 **kwargs 是 fn 的参数",
      "    results = [] : 使用空列表接收线程运行结果 -> [results.append(future.result()) for future in as_completed(futures)]",
      "    \"\"\"",
      "    results = []  # 记录返回结果",
      "    futures = [executor.submit(do_cpu_work, i) for i in range(5)]",
      "",
      "    for future in as_completed(futures):",
      "        result = future.result()",
      "        results.append(result)",
      "",
      "print(results)",
      ""
    ],
    "description": ["创建 ProcessPoolExecutor 进程池"]
  },
  "ThreadPoolExecutor 通用的多线程函数运行器": {
    "prefix": ["def with ThreadPoolExecutor -concurrent.futures"],
    "body": [
      "def run_threading(*tasks, max_workers: int = 6):",
      "    \"\"\"# ThreadPoolExecutor 通用的多线程函数运行器",
      "    - 无参函数格式 : 函数1, 函数2",
      "    - 带参函数格式 : (函数3, None), (函数4, 参数1), [函数5, 参数1, 参数2]",
      "",
      "    Args:",
      "        *tasks : 参数 tasks 支持的格式 -> run_process(函数1, 函数2, (函数3, None), (函数4, 参数1), [函数5, 参数1, 参数2])",
      "    \"\"\"",
      "    from typing import Any, Callable, Literal  # * 引入 typing 对类型提示的支持",
      "",
      "    with ThreadPoolExecutor(max_workers=max_workers) as executor:",
      "        \"\"\"# ThreadPoolExecutor - 多线程：IO密集型任务（网络、文件、等待外部程序）",
      "        **创建 ThreadPoolExecutor 线程池实 例对象 executor**",
      "        futures = [executor.submit(fn, *args, **kwargs)] : fn 参数是要执行的函数, *args 和 **kwargs 是 fn 的参数",
      "        results = [] : 使用空列表接收线程运行结果 -> [results.append(future.result()) for future in as_completed(futures)]",
      "        \"\"\"",
      "        futures = []",
      "        results = []  # 记录返回结果",
      "",
      "        for task in tasks:",
      "            if isinstance(task, Callable):",
      "                func = task",
      "                futures.append(executor.submit(func))",
      "            elif isinstance(task, (list, tuple)):",
      "                func = task[0]",
      "                args = task[1:]",
      "                futures.append(executor.submit(func, *args))",
      "            else:",
      "                print(f'❌ 传入参数错误:\\n\\t{tasks}\\n\\t{task}')",
      "        if futures:",
      "            for future in as_completed(futures):",
      "                try:",
      "                    result = future.result()",
      "                    results.append(result)",
      "                except Exception as e:",
      "                    print(f'❌ 进程执行过程中出现异常: {e}')",
      "    return results",
      ""
    ],
    "description": ["ThreadPoolExecutor 通用的多线程函数运行器"]
  },
  "ProcessPoolExecutor 通用的多进程函数运行器": {
    "prefix": ["def with ProcessPoolExecutor -concurrent.futures"],
    "body": [
      "def run_mutilprocess(*tasks, max_workers: int = 6):",
      "    \"\"\"# ProcessPoolExecutor 通用的多进程函数运行器",
      "    - 无参函数格式 : 函数1, 函数2",
      "    - 带参函数格式 : (函数3, None), (函数4, 参数1), [函数5, 参数1, 参数2]",
      "",
      "    Args:",
      "        *tasks : 参数 tasks 支持的格式 -> run_process(函数1, 函数2, (函数3, None), (函数4, 参数1), [函数5, 参数1, 参数2])",
      "    \"\"\"",
      "    from typing import Any, Callable, Literal  # * 引入 typing 对类型提示的支持",
      "",
      "    with ProcessPoolExecutor(max_workers=max_workers) as executor:",
      "        \"\"\"# ProcessPoolExecutor - 多进程：CPU密集型任务（计算、循环、解析、音视频图像处理）",
      "        **创建 ProcessPoolExecutor 进程池 实例对象 executor**",
      "        futures = [executor.submit(fn, *args, **kwargs)] : fn 参数是要执行的函数, *args 和 **kwargs 是 fn 的参数",
      "        results = [] : 使用空列表接收线程运行结果 -> [results.append(future.result()) for future in as_completed(futures)]",
      "        \"\"\"",
      "        futures = []",
      "        results = []  # 记录返回结果",
      "",
      "        for task in tasks:",
      "            if isinstance(task, Callable):",
      "                func = task",
      "                futures.append(executor.submit(func))",
      "            elif isinstance(task, (list, tuple)):",
      "                func = task[0]",
      "                args = task[1:]",
      "                futures.append(executor.submit(func, *args))",
      "            else:",
      "                print(f'❌ 传入参数错误:\\n\\t{tasks}\\n\\t{task}')",
      "        if futures:",
      "            for future in as_completed(futures):",
      "                try:",
      "                    result = future.result()",
      "                    results.append(result)",
      "                except Exception as e:",
      "                    print(f'❌ 进程执行过程中出现异常: {e}')",
      "    return results",
      ""
    ],
    "description": ["ProcessPoolExecutor 通用的多进程函数运行器"]
  }
}
