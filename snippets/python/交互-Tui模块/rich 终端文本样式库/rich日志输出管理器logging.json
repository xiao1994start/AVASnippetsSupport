{
  "引入 rich.logging.RichHandler 日志记录处理程序": {
    "prefix": ["import rich logging =-rich--basicConfig", "im rich logging =-rich--basicConfig"],
    "body": [
      "import logging",
      "",
      "from rich.logging import RichHandler  # * 引入 rich 模块 logging 日志记录处理程序",
      "",
      "logging.basicConfig(",
      "    level=logging.INFO,  # * 日志级别[默认:WARNING](NOTSET,DEBUG,INFO,WARNING,ERROR,CRITICAL)",
      "    format='%(message)s',",
      "    datefmt='[%X]',",
      "    handlers=[RichHandler(rich_tracebacks=True)],",
      ")",
      ""
    ],
    "description": ["引入 rich.logging.RichHandler 日志记录处理程序", "\n替换默认的 logging.basicConfig.handlers"]
  }
}
