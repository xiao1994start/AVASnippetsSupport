{
  "机器人复制": {
    "prefix": ["robocopy "],
    "body": [
      "@REM * 帮助文档：[https://learn.microsoft.com/zh-cn/windows-server/administration/windows-commands/robocopy]",
      "@REM robocopy 源目录 目标目录:[当前目录使用 %~dp0 时不要加 \"双引号\" 或 使用 . 代替]",
      "@REM [文件：默认\"*.*\" 或 文件夹：/E 复制所有子目录，包括空子目录]",
      "@REM /MIR（慎用）:镜像模式。完全同步源和目标，会将目标文件夹中源文件夹不存在的文件删除",
      "@REM /IS:强制覆盖目标文件",
      "@REM /MOV:移动文件(复制后从源中删除) 或 /MOVE:移动文件和目录(复制后从源中删除)",
      "@REM /MT:[n]（建议：默认，不添加）:使用 n 个线程创建多线程副本",
      "@REM = 使用 robocopy 时，源目录 和 目标目录 的结尾不能存在 \\，否则会报错",
      "robocopy ${1:src源路径} ${2:.} ${3|\"*.*\",/E|} /J /IS /MT:16",
      "$0"
    ],
    "description": ["机器人复制"]
  }
}
